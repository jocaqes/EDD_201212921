using System.ServiceModel;

namespace WSnaval_wars
{
    // NOTE: You can use the "Rename" command on the "Refactor" menu to change the interface name "INavalWars" in both code and config file together.
    [ServiceContract]
    public interface INavalWars
    {

    }
}
